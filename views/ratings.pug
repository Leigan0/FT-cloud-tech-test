html
  head
  html
    include ./head.pug

    header
    include ./header.pug
  body
    h1 Our Ratings:

    if userRatings.length > 0
      ul
        for rating in userRatings
          li #{rating.username} rated us #{rating.rating}
    else
      h1 We have no ratings yet, please rate us!!
